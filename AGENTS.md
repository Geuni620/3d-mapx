# 저장소 가이드라인

## 협업 모드

- 이 저장소에서는 기본적으로 `pair-programming` 계약을 따른다.
- 코드를 논의할 때는 먼저 읽기 전용으로 확인한다. `rg`, `sed`, `ls`, `git status`, `git diff`, 집중적인 파일 읽기를 우선 사용한다.
- 사용자가 현재 대화에서 명시적으로 승인하지 않은 경우, 파일 수정, 파일 생성, 파일 삭제, 포매터 실행, 개발 서버 시작, 패키지 설치, 테스트/빌드 실행, 커밋 생성을 하지 않는다.
- 사용자가 적용, 구현, 수정, 업데이트, 패치 등 변경을 명확히 승인하면 승인된 변경만 수행한다.
- 승인이 모호하면 정확한 파일과 범위를 짚어 짧게 확인 질문을 한다.
- 사용자의 관련 없는 변경사항은 보존한다. 사용자가 명시적으로 요청하지 않는 한 내가 만들지 않은 작업을 되돌리지 않는다.
- 사용자가 달리 요청하지 않는 한 한국어로 협업한다. 설명은 직접적이고 구체적으로 작성하며 로컬 파일과 연결한다.

## 프로젝트 방향

- 이 프로젝트는 한국 모빌리티 데이터를 위한 웹 우선 3D 지도 서비스다.
- 제품 비전과 단계별 방향은 `VISION.md`를 기준으로 최신 상태를 유지한다.
- `AGENTS.md`에는 에이전트 작업 규칙과 짧은 방향만 둔다. 상세한 제품 판단, 데이터 소스, 로드맵은 `VISION.md`에 기록한다.

## 기술 방향

- 프론트엔드: React, TypeScript, Vite.
- 지도 기반: `react-map-gl/maplibre`, `maplibre-gl`.
- 우선순위는 MapLibre 기반 지도, 서울/한국 지하철 네트워크 레이어, 이후 시뮬레이션과 운영 콘솔 순서다.
- deck.gl은 대량 경로, trip, heatmap, arc, animation overlay가 필요해질 때 추가한다.
- Three.js는 일반 지도 레이어로 다루기 어려운 HMI, 센서 시각화, point cloud, custom 3D object가 필요할 때 추가한다.
- `useMemo`와 `useCallback`은 기본값으로 사용하지 않는다. 계산 비용이 크거나 참조 안정성이 실제 렌더링/구독 비용에 영향을 줄 때만 근거를 두고 사용한다.

## 파일/디렉터리 네이밍

- 새로 생성하는 파일과 디렉터리는 기본적으로 `kebab-case`를 사용한다.
- React 컴포넌트를 담는 파일과 컴포넌트 모듈 이름도 예외 없이 `kebab-case`를 사용한다.

## 문서화

- `VISION.md`: 제품 비전, 단계별 로드맵, 데이터 소스, 주요 기술 판단을 기록한다.
- `README.md`: 프로젝트 실행 방법과 사용자 관점의 간단한 소개를 유지한다.
- 중요한 의사결정은 코드에 흩뿌리지 말고 먼저 `VISION.md`에 정리한다.
- 코드 주석은 구현을 그대로 풀어쓰기보다, 소스만으로 드러나지 않는 의도, 제약, 판단 근거를 설명할 때 사용한다.
- 코드 주석은 한국어로 간결하게 작성한다. 특정 주석 형식을 강제하지 말고, 함수 설명, 후속 작업, 공개 API 설명 등 목적에 맞는 표현을 선택한다.

## 명령어

- `pnpm dev`: Vite 개발 서버를 시작한다.
- `pnpm build`: TypeScript 빌드와 Vite 프로덕션 빌드를 실행한다.
- `pnpm lint`: Oxlint를 실행한다.

현재 Oxlint는 linter로 설정되어 있다. `oxfmt`나 Prettier 같은 formatter를 명시적으로 추가하고 설정하기 전까지 저장 시 자동 포맷은 보장되지 않는다.
